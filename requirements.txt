# Core image / RAW
rawpy==0.22.0
numpy==1.26.4
opencv-python==4.9.0.80
Pillow==10.3.0

# Lens correction
lensfunpy==1.13.0

# Colour science / metrics
colour-science==0.4.4
scikit-image==0.23.2

# Deep learning — install torch BEFORE this file via the correct index:
#   CPU:  pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cpu
#   CUDA: pip install torch==2.3.0 torchvision==0.18.0 --index-url https://download.pytorch.org/whl/cu121
# Both targets are handled in Dockerfile and setup.sh. Listing here just for auditing.
torch==2.3.0
torchvision==0.18.0

# Perceptual metric
lpips==0.1.4

# HDR metrics
pu21==0.1.0

# Config / CLI
PyYAML==6.0.1
tqdm==4.66.4
click==8.1.7

# Structured logging
structlog==24.2.0

# Visualisation / contact sheets
matplotlib==3.9.0

# Dev / test
pytest==8.2.2
pytest-cov==5.0.0
